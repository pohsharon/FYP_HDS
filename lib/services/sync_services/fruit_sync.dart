// Fruit sync removed — placeholder stub so sync framework remains compilable.
import 'dart:async';

class SyncFruits {
  Future<void> syncFruits() async {
    // Placeholder: fruit syncing is intentionally disabled/removed.
    // Implement replacement sync logic here when ready.
    return Future.value();
  }
}
